# 自然叠框版本备份

1. `debug_build_alarm_video_from_local_natural_drawbox_backup.py` 是本地实验脚本的“自然叠框版本”。
2. 这个版本的逻辑是从报警截图/红框中提取 bbox。
3. 它会从常态录像中截取报警点前后 30 秒。
4. 它会在 `alarm_second` 前 1 秒到后 2 秒，共约 3 秒内叠加红框。
5. 这个版本暂不接入正式生产链路。
6. 后续如果替换报警截图方案效果不好，可以回退参考这个备份文件。
